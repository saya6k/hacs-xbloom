# Must match the devcontainer image tag and hacs.json's `homeassistant` floor
# — one version string across all three. The devcontainer and CI use the
# image's preinstalled copy because dev nightlies may not be on PyPI yet.
homeassistant>=2026.9.0.dev202608241354
pytest

# Transitive imports of HA's `usb` integration, reached as soon as a test
# imports anything under homeassistant.components. The container image ships
# them; a bare pip install does not, and without them all 37 test modules
# that touch HA fail to collect.
aiousbwatcher
serialx
