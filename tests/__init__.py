"""Unit tests for the xbloom custom integration.

Pure-logic tests only (schema helpers, migrations, resolvers) — no HA
core fixtures, no BLE. Requires the ``homeassistant`` package to be
importable (installed by ``scripts/setup`` in the devcontainer, or any
venv with HA).
"""
